<nav class="flex items-center justify-between flex-wrap bg-black p-6">
  <div class="flex items-center flex-no-shrink text-white mr-6">
    <span class="font-semibold text-xl tracking-tight">AV</span>
  </div>
  <div class="block lg:hidden">
    <button
      class="flex items-center px-3 py-2 border rounded text-white border-white hover:text-white"
      onclick={{action "toggleMenu"}}>
      <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
        <title>Menu</title>
        <path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/>
      </svg>
    </button>
  </div>
  {{#if (or media.isDesktop showMenu)}}
    <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">
      <div class="text-sm lg:flex-grow">
        {{#link-to "counter" class="block mt-4 lg:inline-block lg:mt-0 text-white hover:text-white mr-4 no-underline"}}
          Cube of Truth Counter
        {{/link-to}}
        {{!-- {{#link-to "events" class="block mt-4 lg:inline-block lg:mt-0 text-white hover:text-white no-underline"}}
          Events
        {{/link-to}} --}}
      </div>
    </div>
  {{/if}}
</nav>
